# 2026-07-22 开盘结算勘误

本次虚拟成交结算状态为 **FAILED_DAILY_TRADE**，没有生成成交，也没有成功写入取消事件。

`data/inbox/2026-07-22-open.json` 的采集备注写成了“两笔待执行订单已取消”，但该表述只代表失败关闭时的原定动作，不代表账本事务已经提交。权威账本 `data/ledger/portfolio.json` 与 `data/ledger/orders.jsonl` 显示，两笔订单仍为 `PENDING_NEXT_OPEN`：

- `510050` 华夏上证50ETF，1900 份；
- `512170` 华宝中证医疗ETF，7300 份。

原因有两层：09:30—09:35 没有保存合格的双源开盘快照；随后仓库 Git 元数据又处于 WMI 安全隔离状态，来源校验按规则失败关闭，账本没有发生部分写入。

原始输入属于不可变证据，不回填、不改写。本勘误文件负责纠正其解释。待 Git 元数据由外部安全流程恢复后，再在受治理的交易周期中处理过期订单；不得伪造缺失的开盘数据。

Git 持久化状态：`WMI_GIT_DEFERRED`。本文件先保存在本地，统一等待北京时间 2026-07-23 02:00 的持久化门检查。
