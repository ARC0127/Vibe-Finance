# Vibe Finance 本地文档与日志整理报告

- 整理截点：2026-07-20 16:35:27 CST
- 方式：Headroom local-first 增量清点；原文和历史报告保持可回溯
- 范围：根规范文档、`docs/`、`config/`、`reports/`、`data/inbox/`、`data/research/`、`data/ledger/`
- 本报告、滚动索引及 README 的索引入口在清点完成后新增，不计入本次 25 文件基线
- 最终复核：2026-07-20 16:38:17 CST

## 结果

| 分类 | 文件数 |
|---|---:|
| 当前规范文档 | 10 |
| 机器配置/模式锁 | 4 |
| 点时输入与研究数据 | 2 |
| 账本与审计日志 | 3 |
| 决策报告 | 4 |
| 研究报告 | 2 |
| 合计 | 25 |

- 基线总字节数：97,437。
- Markdown/JSON/JSONL 编码与解析：25/25 通过。
- 本地 Markdown 链接：16 个，断链 0。
- 完全相同内容：0 组。
- 机器决策报告 run_id：2 个；账本中均可找到，未匹配 0。
- 虚拟持仓 0、待执行订单 0；因此 `data/ledger/orders.jsonl` 尚未创建，属于预期状态。
- 仓库密钥模式扫描：0 个命中；`.env` 等敏感文件不进入清单。

完整逐文件路径、大小、修改时间、SHA-256 与解析状态见[机器清单](2026-07-20-1635.json)。

## 本次规范整理

1. 将 `MODE_LOCK.md` 和 `MODE_LOCK.json` 与当前 8 个 ACTIVE 定时同步，补充场外基金未知价结算、文档整理任务边界，以及策略参数升级至少 20 笔完成交易的门禁。
2. 将 `docs/SOURCES.md` 的初始 v0.1 TLS/访问失败改为带日期的历史边界；保留 2026-07-20 午间 C 级端点许可状态 `UNKNOWN`、不可用于订单的事实。
3. 新增 `docs/GITHUB_AUTOMATION.md`，定义每个定时对应 Git 提交、长期 Draft PR、自主进化证据、互斥锁、allowlist 暂存和回滚要求。
4. 新增 `docs/DOCUMENT_LOG_INDEX.md`，集中给出当前规范、配置、账本、点时数据、历史报告和整理报告入口。

## 保留的历史语境

- `reports/research/2026-07-19-analysis.md` 只分析三只初始 ETF，属于当时研究范围，不代表当前 20 只基金研究池和 30 只股票动态短名单。
- `reports/research/2026-07-20-preopen.md` 含多次同日追加，并记录当时“7 项测试通过”和 v0.1 回滚基线。它们是点时历史，不应为了匹配当前 15 项测试而回写。
- 后续追加研究应优先生成新的带时间戳附录，避免一个历史文件持续增长导致截点边界模糊。

## 截点后并发变化

16:30 收盘定时在本次 16:35 基线之后完成，新增 `data/inbox/2026-07-20.json`、`reports/daily/2026-07-20-short.md/json`，并按正常金融流水线更新账本与心跳。新 run_id 已进入账本，持仓和待执行订单仍为 0。它们已加入滚动索引，但没有倒填进本报告的 25 文件哈希基线。

## GitHub 自动更新状态

- `origin` 已配置为 `ARC0127/Vibe-Finance`。
- 本地 Git 作者身份已配置。
- GitHub CLI 当前未安装；HTTPS 远端凭据不可用，`git ls-remote origin HEAD` 认证失败。
- 因此自动推送保持 `BLOCKED_NOT_ENABLED`，8 个定时尚未被改成失败式推送。
- 启用条件：安装并登录 `gh`、核验仓库 private 与 push 权限、建立持久自动化分支及 Draft PR、验证非交互凭据和跨任务互斥锁。

## 自主进化记录原则

每次反思任务都应留下 `PROPOSED_ONLY / ACCEPTED / REJECTED` 之一。策略改变只有在至少 20 笔已完成虚拟交易、走前和独立样本外验证通过、费用压力测试通过、最大回撤不恶化及全部测试成功时才可进入同一 Git 提交；否则只提交候选报告。GitHub 提交历史用于审计与回滚，不能代替统计证据。

## 后续建议

1. 安装 GitHub CLI 并完成同一自动化用户下的安全登录；完成前不要在 Prompt 或仓库中保存令牌。
2. 首次启用 GitHub 同步时，先对一个低风险文档任务做 dry run，再扩展到全部定时。
3. 第一次实际订单、基金净值结算或进化候选产生时，再创建对应目录；不预造空证据。
