# Vibe Finance 本地文档与日志整理报告

- 整理截点：2026-07-20 23:18:35 CST
- 截点复核：2026-07-20 23:52:50 CST；期间受控范围无文件变化
- 方式：Headroom local-first 增量清点；原文件与历史报告保持不可变
- 范围：`README.md`、`MASTER_PROMPT.md`、`MODE_LOCK.md/json`、`docs/`、`config/`、`reports/`、`data/research/`、`data/ledger/`，以及存在时的 `orders.jsonl`
- 基线：automation memory 为首次运行；使用不可变的 `reports/document-log/2026-07-20-1635.json` 作为辅助差异基线
- 本报告、对应 JSON、索引更新与随后由同步脚本生成的运行清单发生在截点之后，不倒填本次 37 文件哈希基线

## 清点结果

| 分类 | 文件数 |
|---|---:|
| 规范文档 | 11 |
| 配置 | 4 |
| 原始研究数据 | 1 |
| 决策报告 | 9 |
| 成交/基金结算报告 | 2 |
| 进化报告 | 0 |
| 运行日志 | 10 |
| 合计 | 37 |

- 总字节数：150,932。
- JSON/JSONL/Markdown 解析：37/37 通过；解析失败 0。
- Markdown 本地链接：55 个；断链 0。
- 文件名/关联日期明显冲突：0。
- SHA-256 完全重复内容：0 组。
- 报告 `run_id`：4 个，均可在账本运行历史中匹配；未匹配 0。
- 报告订单引用：0；账本待执行订单：0；`orders.jsonl` 尚未创建，当前一致。
- 敏感文件名规则排除：0 个命中；未读取范围外 `.env`、密钥、凭据或浏览器数据。

逐文件相对路径、类型、字节数、修改时间、SHA-256、关联日期与解析状态见[机器清单](2026-07-20-2352.json)。

## 增量差异

相对 16:35 不可变辅助基线：新增 13 个、修改 4 个、未变 20 个。辅助基线中的 `data/inbox/2026-07-19.json` 不在本轮用户指定清点范围，因此记为范围差异，不视为删除。

修改项为 `README.md`、`docs/GITHUB_AUTOMATION.md`、`data/ledger/heartbeat.json` 和 `data/ledger/portfolio.json`；后两项在本任务开始前已由基金流水线修改，本任务只读保留，没有刷新心跳或账本。

新增项主要包括 2026-07-20 短周期报告、收盘审计、基金报告、活动监测、上次文档整理报告及其 GitHub 自动化运行清单。

## 审计发现与索引修复

1. 旧滚动索引未列出 `reports/research/2026-07-20-close-audit.md` 与 `reports/funds/2026-07-20-funds.md/json`；基金 Markdown 因此形成一个明显孤立入口。本轮仅原子更新滚动索引，未改写这些历史文件。
2. 旧索引仍把 `reports/funds/` 与 `reports/monitor/` 列为尚未产生；当前文件已存在。本轮移除陈旧说明并加入现有入口。
3. 同日多版本包括 3 份不同时点的 document-log 自动化运行清单、2026-07-19 short/long 两类日报，以及 2026-07-20 盘前/收盘两阶段研究报告。它们的用途、时点或模式不同，哈希也不同，不构成重复内容。
4. 没有发现报告与账本 `run_id`、订单引用之间的不一致；未据此推断任何金融结论。

## 归档与去重建议

本轮没有证据充分的归档或去重候选。上述同日文件应继续保留为不可变审计证据；如需压缩历史运行清单，应由用户另行确认范围和保留策略，本任务未删除、移动、改名或压缩任何文件。

## 边界确认

- 未进行行情研究、投资分析、虚拟下单或策略修改。
- 未调用 DeepSeek 或其他付费 API。
- 未更新金融流水线心跳、账本或订单。
- 工作区开始时已有的未提交基金流水线文件全部保留。

